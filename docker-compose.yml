version: '3.8'

services:
  flow-manager:
    build: .
    container_name: flow-manager-api
    ports:
      - "8000:8000"
    environment:
      - DEBUG=False
      - LOG_LEVEL=INFO
    volumes:
      - ./app:/app/app
    restart: unless-stopped